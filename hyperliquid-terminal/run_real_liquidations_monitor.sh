#!/bin/bash

# Скрипт для запуска монитора реальных позиций на грани ликвидации

echo "🎯 Запуск монитора реальных позиций на грани ликвидации..."

# Переходим в директорию с файлами
cd "$(dirname "$0")"

# Проверяем наличие Python 3
if ! command -v python3 &> /dev/null; then
    echo "❌ Python 3 не найден. Пожалуйста, установите Python 3."
    exit 1
fi

# Проверяем наличие необходимых модулей
echo "🔍 Проверка зависимостей..."
python3 -c "import aiohttp, ssl, certifi" 2>/dev/null
if [ $? -ne 0 ]; then
    echo "📦 Установка зависимостей..."
    pip3 install aiohttp certifi
fi

echo "✅ Зависимости готовы!"
echo ""

# Запускаем монитор
echo "🚀 Запуск монитора реальных ликвидаций..."
python3 real_liquidations_monitor.py