# Hyperliquid Positions at Risk Monitor

## Обзор

Новая система мониторинга для отслеживания крупных позиций близких к ликвидации на платформе Hyperliquid. В отличие от существующего монитора ликвидаций, который отслеживает уже произошедшие ликвидации, этот монитор фокусируется на превентивном анализе рисков.

## Основные возможности

### 🎯 Мониторинг рисковых позиций

- **Отслеживание изменений открытого интереса (Open Interest)**
- **Анализ уровней ликвидации для крупных позиций**  
- **Мониторинг давления от фандинга**
- **Предупреждения о высоких рисках ликвидации**

### 📊 Типы предупреждений

1. **OI_SURGE** - Резкое увеличение открытого интереса
2. **LIQUIDATION_RISK** - Позиции в зоне риска ликвидации
3. **FUNDING_PRESSURE** - Высокие ставки фандинга
4. **HIGH_LEVERAGE_RISK** - Риски высокого плеча

## Быстрый старт

### Простой мониторинг

```bash
# Запуск простого мониторинга
./run_positions_risk_monitor.sh

# Или напрямую
python3 positions_at_risk_monitor_simple.py
```

### Продвинутый анализ

```bash
# Запуск с полным анализом рисков
./run_positions_risk_monitor.sh -m advanced

# Или напрямую
python3 positions_at_risk_monitor.py
```

### Фоновый режим

```bash
# Запуск в фоне с логированием
./run_positions_risk_monitor.sh -b -l monitor.log

# Остановка фонового процесса
./run_positions_risk_monitor.sh --stop positions_risk_monitor_simple.pid
```

## Конфигурация

### Основные настройки (positions_at_risk_config.py)

```python
# Отслеживаемые активы
MONITORED_ASSETS = ["BTC", "ETH", "SOL"]

# Пороговые значения
MIN_OPEN_INTEREST_USD = 1000000  # Минимальный ОИ для анализа
OPEN_INTEREST_CHANGE_THRESHOLD = 0.15  # 15% изменение ОИ
LIQUIDATION_BUFFER_PERCENT = 5.0  # 5% до ликвидации
MARGIN_RISK_THRESHOLD = 15  # 15x плечо для предупреждений

# Интервалы
POLL_INTERVAL_SECONDS = 10  # Проверка каждые 10 секунд
```

### Настройка простого монитора

```python
# Более мягкие настройки для простого монитора
MIN_OPEN_INTEREST_USD = 500000
OPEN_INTEREST_CHANGE_THRESHOLD = 0.20  # 20% изменения
LIQUIDATION_BUFFER_PERCENT = 8.0  # 8% буфер
POLL_INTERVAL_SECONDS = 15  # Реже проверки
```

## Примеры вывода

### 1. Регулярный статус рынка (каждую минуту)

```
[16:02:10] 📊 Market Status:
  BTC    | Price: $ 87,387.00 | OI: $1,911,329,677 (+0.0%) | Funding: 🟢 +0.0007% | Premium: -0.032%
  ETH    | Price: $  2,908.60 | OI: $2,466,851,467 (-0.0%) | Funding: 🟢 +0.0013% | Premium: -0.024%
  SOL    | Price: $    127.35 | OI: $ 425,539,111 (+0.1%) | Funding: 🟢 +0.0013% | Premium: -0.047%
✅ No critical risks detected
```

**Индикаторы фандинга:**
- 🟢 Зеленый: < 0.05% (нормальный уровень)
- 🟡 Желтый: 0.05% - 0.1% (умеренное давление)
- 🔴 Красный: > 0.1% (высокое давление)

### 2. Резкое изменение Open Interest

```
============================================================
📈 OI SURGE DETECTED - BTC
============================================================
Previous OI: $523,450,000
Current OI:  $678,920,000
Change:      +29.7%
Current Px:  $89,250.00
============================================================
```

### 3. Риск ликвидации

```
============================================================
🚨 RISK ALERT - ETH
============================================================
Type: LIQUIDATION_RISK
Severity: HIGH
Description: Large LONG positions at risk (within 4.2% of liquidation)

📈 Market Data:
   Current Price: $2,580.25
   Liquidation Price: $2,472.60
   Distance: 4.35%
   Estimated Size: $15,750,000
   Side: LONG
   Max Leverage: 25x
============================================================
```

### 4. Давление фандинга

```
============================================================
💰 FUNDING STRESS - SOL
============================================================
Funding Rate: +0.0820%
Premium:      +0.156%
Price:        $98.45
============================================================
```

## Алгоритмы анализа

### 1. Расчет уровней ликвидации

```python
# Приближение на основе initial margin (= notional / leverage) и maintenance margin

# Для лонг позиции:
liquidation_price = entry_price * (1 - 1 / leverage) / (1 - maintenance_margin_rate)

# Для шорт позиции:
liquidation_price = entry_price * (1 + 1 / leverage) / (1 + maintenance_margin_rate)
```

### 2. Определение крупных позиций

- Минимум $1M USD открытого интереса
- Анализ позиций размером от 5% от общего ОИ
- Расчет потенциальных уровней ликвидации

### 3. Мониторинг рисковых факторов

- **Изменения ОИ > 15%** - потенциальная концентрация позиций
- **Ставки фандинга > 0.05%** - давление на маржинальных трейдеров
- **Премии > 0.5%** - отклонения справедливой цены
- **Расстояние до ликвидации < 5%** - критические риски

## Различия с монитором ликвидаций

| Параметр | Монитор ликвидаций | Positions at Risk |
|----------|-------------------|-------------------|
| **Фокус** | Уже произошедшие ликвидации | Потенциальные риски |
| **Данные** | recentTrades с liquidation=true | metaAndAssetCtxs + расчеты |
| **Реактивность** | Постфактум | Превентивно |
| **Польза** | Исторический анализ | Раннее предупреждение |

## API Endpoints

### Основные эндпоинты

```bash
# Получение метаданных рынка
curl -X POST https://api.hyperliquid.xyz/info \
  -H "Content-Type: application/json" \
  -d '{"type": "meta"}'

# Получение контекста активов (цены, ОИ, фандинг)
curl -X POST https://api.hyperliquid.xyz/info \
  -H "Content-Type: application/json" \
  -d '{"type": "metaAndAssetCtxs"}'
```

### Ответы API

```json
// Meta response
{
  "universe": [
    {
      "name": "BTC",
      "maxLeverage": 40,
      "marginTableId": 56
    }
  ]
}

// Asset context
{
  "markPx": "89250.00",
  "openInterest": "21615.21516", 
  "funding": "0.0000125",
  "premium": "-0.000277408"
}
```

## Системные требования

- Python 3.7+
- aiohttp для асинхронных HTTP запросов
- Стабильное интернет-соединение
- 50MB RAM (простой) / 100MB RAM (продвинутый)

## Интеграция с systemd

### Создание сервиса

```bash
sudo cp positions_at_risk_monitor.service /etc/systemd/system/
sudo systemctl enable positions_at_risk_monitor
sudo systemctl start positions_at_risk_monitor
```

### Управление сервисом

```bash
# Проверка статуса
sudo systemctl status positions_at_risk_monitor

# Просмотр логов
sudo journalctl -u positions_at_risk_monitor -f

# Перезапуск
sudo systemctl restart positions_at_risk_monitor
```

## Устранение неполадок

### Проблемы с подключением

```bash
# Проверка API
curl -X POST https://api.hyperliquid.xyz/info -d '{"type":"meta"}'

# Проверка зависимостей
python3 -c "import aiohttp; print('aiohttp OK')"
```

### Высокая нагрузка на CPU

- Увеличьте `POLL_INTERVAL_SECONDS`
- Используйте простой режим мониторинга
- Уменьшите количество отслеживаемых активов

### Ложные предупреждения

- Увеличьте пороговые значения в конфигурации
- Настройте `ALERT_CONSECUTIVE_COUNT`
- Используйте более консервативные настройки

## Разработка и расширение

### Добавление новых активов

```python
# В positions_at_risk_config.py
MONITORED_ASSETS = ["BTC", "ETH", "SOL", "ARB", "OP"]
```

### Кастомные алерты

```python
# Переопределите display_risk_alert()
class CustomRiskMonitor(PositionsAtRiskMonitor):
    def display_risk_alert(self, risk_factor):
        # Кастомная логика уведомлений
        send_webhook(risk_factor)
        save_to_database(risk_factor)
```

### Интеграция с базой данных

```python
# Сохранение метрик в базу
async def save_risk_metrics(self, asset, metrics):
    # SQLite, PostgreSQL, InfluxDB, etc.
    pass
```

## Лицензия

Система распространяется под лицензией MIT. См. файл LICENSE для деталей.

## Поддержка

Для вопросов и предложений:

1. Создайте issue в репозитории
2. Проверьте существующие документацию
3. Изучите логи и выходные данные для отладки

---

**Примечание**: Данная система предназначена для аналитических целей и не является инвестиционной рекомендацией. Всегда проводите собственный анализ перед принятием торговых решений.