# Hyperliquid Liquidations Monitor

Отдельный скрипт для мониторинга крупных ликвидаций на Hyperliquid по активам BTC, ETH и SOL.

## Описание

Скрипт в реальном времени отслеживает ликвидации позиций на бирже Hyperliquid и выводит информацию о крупных ликвидациях, превышающих заданный порог.

## Возможности

- ✅ Мониторинг ликвидаций в реальном времени для BTC, ETH, SOL
- ✅ Настраиваемый порог минимальной суммы ликвидации (по умолчанию $50,000)
- ✅ Отображение ключевой информации:
  - Время ликвидации
  - Актив и сторона позиции (LONG/SHORT)
  - Цена исполнения
  - Размер позиции
  - Стоимость в USD
- ✅ Асинхронная архитектура для эффективной работы
- ✅ Автоматическое переподключение при сбоях

## Установка зависимостей

```bash
pip install aiohttp websockets certifi
```

Или установите все зависимости из requirements.txt:

```bash
pip install -r requirements.txt
```

## Использование

### Базовый запуск (порог $50,000):

```bash
python3 liquidations_monitor.py
```

### Запуск с пользовательским порогом:

```bash
# Отслеживать ликвидации от $100,000
python3 liquidations_monitor.py 100000

# Отслеживать ликвидации от $10,000
python3 liquidations_monitor.py 10000
```

### Запуск в фоновом режиме:

```bash
# С выводом в файл
python3 liquidations_monitor.py > liquidations.log 2>&1 &

# Без вывода
nohup python3 liquidations_monitor.py &
```

## Пример вывода

```
================================================================================
🔴 HYPERLIQUID LIQUIDATIONS MONITOR
================================================================================
Monitoring: BTC, ETH, SOL
Threshold: $50,000
Started at: 2024-01-15 10:30:45
================================================================================

Current prices:
  BTC: $43,250.00
  ETH: $2,580.50
  SOL: $98.75

================================================================================
🚨 LIQUIDATION DETECTED - BTC
================================================================================
Time:       2024-01-15 10:31:23
Type:       LONG liquidation
Price:      $43,180.00
Size:       2.5000 BTC
USD Value:  $107,950.00
Side:       SELL
================================================================================
```

## Настройка

Вы можете изменить параметры в начале файла `liquidations_monitor.py`:

```python
# Отслеживаемые активы
MONITORED_ASSETS = ["BTC", "ETH", "SOL"]

# Минимальный порог ликвидации в USD
LIQUIDATION_THRESHOLD_USD = 50000
```

## API Endpoints

Скрипт использует публичный API Hyperliquid:

- REST API: `https://api.hyperliquid.xyz/info`
- WebSocket: `wss://api.hyperliquid.xyz/ws`

### Основные типы запросов:

- `meta` - метаданные рынка и список активов
- `allMids` - текущие средние цены
- `recentTrades` - последние сделки (включая ликвидации)

## Технические детали

- **Язык:** Python 3.8+
- **Асинхронность:** asyncio, aiohttp
- **Интервал опроса:** 2 секунды
- **Timeout запросов:** 15 секунд
- **SSL:** Использует certifi для безопасного соединения

## Остановка скрипта

Нажмите `Ctrl+C` для корректного завершения работы.

## Примечания

- Скрипт **независим** от основного Hyperliquid Terminal и не влияет на его работу
- Не требует аутентификации или API ключей (использует публичные данные)
- Работает 24/7, автоматически восстанавливается после ошибок
- Отслеживает только новые ликвидации (избегает дубликатов)

## Troubleshooting

### Скрипт не запускается:
```bash
# Проверьте Python версию
python3 --version  # должна быть 3.8+

# Переустановите зависимости
pip install -r requirements.txt --upgrade
```

### Нет данных о ликвидациях:
- Это нормально в периоды низкой волатильности
- Попробуйте снизить порог (например, до $10,000)
- Проверьте доступность API: `curl https://api.hyperliquid.xyz/info`

### Ошибки подключения:
- Проверьте интернет-соединение
- Убедитесь, что Hyperliquid API доступен
- Скрипт автоматически попытается переподключиться через 5 секунд

## Лицензия

Используйте в соответствии с лицензией основного проекта.
