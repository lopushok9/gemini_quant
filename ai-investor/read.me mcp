# Alpha Vantage MCP Server

MCP (Model Context Protocol) сервер для получения данных о акциях через Alpha Vantage API.

## 📋 Возможности

- Получение обзорной информации о компании (OVERVIEW)
- Получение последних новостей о компании (NEWS_SENTIMENT)
- Интеграция с Claude Desktop для анализа акций

## 🔧 Требования

- Python 3.8+
- Alpha Vantage API ключ ([получить бесплатно](https://www.alphavantage.co/support/#api-key))
- Claude Desktop (для продакшн использования)

## 📦 Установка

### 1. Клонируйте репозиторий или создайте проект

```bash
mkdir ai-investor
cd ai-investor
```

### 2. Создайте виртуальное окружение

```bash
python3 -m venv venv
source venv/bin/activate  # На macOS/Linux
# или
venv\Scripts\activate  # На Windows
```

### 3. Установите зависимости

```bash
pip install mcp requests
```

### 4. Создайте файл `mcp_server.py`

Скопируйте код сервера в файл `mcp_server.py`.

### 5. Получите Alpha Vantage API ключ

1. Перейдите на https://www.alphavantage.co/support/#api-key
2. Заполните форму и получите бесплатный ключ
3. Сохраните ключ в безопасном месте

## 🚀 Использование

### Вариант 1: Интеграция с Claude Desktop (Рекомендуется)

#### macOS

1. Откройте или создайте конфигурационный файл:
```bash
nano ~/Library/Application\ Support/Claude/claude_desktop_config.json
```

2. Добавьте конфигурацию сервера:
```json
{
  "mcpServers": {
    "alpha-vantage": {
      "command": "/полный/путь/к/venv/bin/python",
      "args": [
        "/полный/путь/к/mcp_server.py"
      ],
      "env": {
        "ALPHA_VANTAGE_API_KEY": "ваш_api_ключ_здесь"
      }
    }
  }
}
```

**Пример:**
```json
{
  "mcpServers": {
    "alpha-vantage": {
      "command": "/Users/username/ai-investor/venv/bin/python",
      "args": [
        "/Users/username/ai-investor/mcp_server.py"
      ],
      "env": {
        "ALPHA_VANTAGE_API_KEY": "YOUR_ALPHA_VANTAGE_KEY"
      }
    }
  }
}
```

#### Windows

1. Откройте конфигурационный файл:
```
%APPDATA%\Claude\claude_desktop_config.json
```

2. Добавьте конфигурацию:
```json
{
  "mcpServers": {
    "alpha-vantage": {
      "command": "C:\\path\\to\\ai-investor\\venv\\Scripts\\python.exe",
      "args": [
        "C:\\path\\to\\ai-investor\\mcp_server.py"
      ],
      "env": {
        "ALPHA_VANTAGE_API_KEY": "ваш_api_ключ_здесь"
      }
    }
  }
}
```

3. **Перезапустите Claude Desktop**

4. **Проверьте подключение:**
   - Откройте Claude Desktop
   - В правом нижнем углу должна появиться иконка 🔌 с подключенными серверами
   - Попросите Claude: "Получи данные по акции AAPL"

### Вариант 2: Тестирование с MCP Inspector

#### Установка Inspector

```bash
npm install -g @modelcontextprotocol/inspector
```

#### Запуск с API ключом

**macOS/Linux:**
```bash
export ALPHA_VANTAGE_API_KEY="your_key_here"
npx @modelcontextprotocol/inspector ./venv/bin/python mcp_server.py
```

**Windows:**
```cmd
set ALPHA_VANTAGE_API_KEY=your_key_here
npx @modelcontextprotocol/inspector venv\Scripts\python.exe mcp_server.py
```

#### Тестирование в Inspector

1. Откройте браузер по адресу, указанному в терминале (обычно `http://localhost:6274`)
2. Выберите инструмент `get_stock_data`
3. Введите параметры:
```json
{
  "ticker": "AAPL"
}
```
4. Нажмите "Call Tool"

## 📖 API Инструмента

### `get_stock_data`

Получает данные о компании по тикеру.

**Параметры:**
- `ticker` (string, обязательный) - Тикер акции (например, "AAPL", "GOOGL", "TSLA")

**Возвращает:**
```json
{
  "status": "success",
  "ticker": "AAPL",
  "overview": {
    "Symbol": "AAPL",
    "Name": "Apple Inc",
    "Description": "...",
    "MarketCapitalization": "3000000000000",
    "PERatio": "28.5",
    ...
  },
  "news": {
    "feed": [
      {
        "title": "Apple announces new product",
        "url": "https://...",
        "time_published": "20241128T120000",
        "summary": "...",
        "overall_sentiment_score": 0.5
      },
      ...
    ]
  }
}
```

**В случае ошибки:**
```json
{
  "status": "error",
  "errors": ["Failed to load OVERVIEW", "News error: ..."],
  "ticker": "AAPL"
}
```

## 💡 Примеры использования в Claude

После настройки в Claude Desktop вы можете спросить:

- "Получи данные по акции Apple (AAPL)"
- "Какие последние новости о Tesla?"
- "Покажи overview для GOOGL"
- "Сравни новости по AAPL и MSFT"

Claude автоматически вызовет MCP сервер и обработает данные.

## ⚠️ Ограничения Alpha Vantage API

Бесплатный план:
- 25 запросов в день
- 5 API вызовов в минуту

При превышении лимита API вернет сообщение о превышении квоты.

## 🐛 Решение проблем

### Ошибка "Missing ALPHA_VANTAGE_API_KEY"

Убедитесь, что API ключ корректно указан в конфигурации Claude Desktop или экспортирован в переменную окружения.

### Сервер не отображается в Claude Desktop

1. Проверьте правильность путей в `claude_desktop_config.json`
2. Убедитесь, что пути абсолютные (не относительные)
3. Перезапустите Claude Desktop
4. Проверьте логи Claude Desktop

### "TypeError: BaseModel.__init__()"

Обновите MCP до последней версии:
```bash
pip install --upgrade mcp
```

### Проблемы с npm при установке Inspector

```bash
npm cache clean --force
sudo chown -R $(whoami) ~/.npm
```

## 📝 Лицензия

MIT

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте раздел "Решение проблем"
2. Убедитесь, что у вас установлены все зависимости
3. Проверьте логи Claude Desktop или MCP Inspector

## 🔗 Полезные ссылки

- [Alpha Vantage Documentation](https://www.alphavantage.co/documentation/)
- [MCP Documentation](https://modelcontextprotocol.io/)
- [Claude Desktop](https://claude.ai/download)